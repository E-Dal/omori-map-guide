# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Node (if we ever add tooling)
node_modules/
package-lock.json

# Local build artifacts
dist/
.cache/

# The map images ARE the site — index.html fetches data/raw_pngs/*.png and
# data/stitched/*.png directly — so they ship. About 140 MB across 636 + 17
# files; well inside GitHub Pages' limits, and there is no site without them.
data/stitched/*.bak-*

# The game's decryption keys. Read at run time instead — see scripts/_keys.py.
scripts/omori_keys.json

*.log
